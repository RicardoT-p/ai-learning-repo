# AI 数据工程师作品集


1. `async_crawler`：`asyncio`、并发限制、重试、robots.txt、JSONL。
2. `scrapy_crawler`：Scrapy 调度、去重、限速、断点续爬。
3. `training_data_pipeline`：清洗、脱敏、精确去重、近似去重、统计。
4. `llm_feedback_eval`：SQLite 反馈库、固定评测集、回归报告。
5. `datatrove_pipeline`：DataTrove 训练语料流水线。
6. `polars_duckdb_quality`：Polars + DuckDB 数据质量分析。
7. `airflow_workflow`：Airflow 工作流编排。
8. `argilla_feedback`：人工反馈与偏好标注。
9. `rag_eval_observability`：Ragas + Phoenix 评测与追踪。
10. `ray_distributed_pipeline`：Ray Data 分布式处理。

